parliament_decision_category = {
	USA_Lobby_Republican = {
		fire_only_once = no
		days_remove = 30

		visible = { original_tag = USA }
		available = { }

		cost = 0
		
		modifier = {
			political_power_gain = -0.25
		}
		remove_effect = {
			set_temp_variable = { party_id = 4 } ## Republicans
			set_temp_variable = { support_to_add = 0.05 }
			change_party_support = yes
		}
	}

	USA_Lobby_Democratic = {
		fire_only_once = no
		days_remove = 30
		cost = 0

		visible = { original_tag = USA	}
		available = { }
		
		modifier = {
			political_power_gain = -0.25
		}
		remove_effect = {
			set_temp_variable = { party_id = 6 } ## Democrats
			set_temp_variable = { support_to_add = 0.05 }
			change_party_support = yes
		}
	}

	USA_Lobby_Progressive = {
		fire_only_once = no
		days_remove = 30
		cost = 0

		visible = { 
			original_tag = USA
			set_temp_variable = { party_id = 7 } ## Progressive
			has_party_in_parliament = yes
			has_country_flag = USA_SocDem_Progressive
		}
		available = { }
		
		modifier = {
			political_power_gain = -0.25
		}
		remove_effect = {
			set_temp_variable = { party_id = 7 } ## Progressive
			set_temp_variable = { support_to_add = 0.05 }
			change_party_support = yes
		}
	}

	USA_Republican_Campaign = {
		fire_only_once = no
		days_remove = 45
		cost = 50

		visible = { original_tag = USA	}
		available = { 
			check_variable = { party_relative_popularity_4 < 0.55 } ## Republicans
		}

		modifier = {
			political_power_gain = -0.15
		}
		remove_effect = {
			set_temp_variable = { party_id = 4 } ## Republicans
			set_temp_variable = { popularity_to_add = 0.02 }
			add_relative_popularity_to_parliament_party = yes
		}
	}

	USA_Democratic_Campaign = {
		fire_only_once = no
		days_remove = 45
		cost = 50

		visible = { original_tag = USA	}
		available = {
			check_variable = { party_relative_popularity_6 < 0.55 } ## Democrats
		}

		modifier = {
			political_power_gain = -0.15
		}
		remove_effect = { #Add 2.00% Conservative Popularity
			set_temp_variable = { party_id = 6 } ## Democrats
			set_temp_variable = { popularity_to_add = 0.02 }
			add_relative_popularity_to_parliament_party = yes
		}
	}

	USA_Progressive_Campaign = {
		fire_only_once = no
		days_remove = 45
		cost = 50

		visible = {
			original_tag = USA
			set_temp_variable = { party_id = 7 } ## Progressive
			has_party_in_parliament = yes
			always = no
		}
		available = {
			check_variable = { party_relative_popularity_7 < 0.55 } ## Progressive
		}

		modifier = {
			political_power_gain = -0.15
		}
		remove_effect = { #Add 2.00% Progressive Popularity
			set_temp_variable = { party_id = 7 } ## Democrats
			set_temp_variable = { popularity_to_add = 0.02 }
			add_relative_popularity_to_parliament_party = yes
		}
	}
}